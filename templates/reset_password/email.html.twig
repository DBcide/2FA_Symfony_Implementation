{# templates/reset_password/email.html.twig #}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Réinitialisation du mot de passe</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
        }
        a.button {
            display: inline-block;
            padding: 10px 20px;
            margin-top: 10px;
            background-color: #0d6efd;
            color: #fff !important;
            text-decoration: none;
            border-radius: 4px;
        }
        a.button:hover {
            background-color: #0b5ed7;
        }
        .container {
            max-width: 600px;
            margin: auto;
            padding: 20px;
        }
        p {
            margin-bottom: 1rem;
        }
    </style>
</head>
<body>
<div class="container">
    <h1>Bonjour !</h1>

    <p>Pour réinitialiser votre mot de passe, veuillez cliquer sur le bouton ci-dessous :</p>

    <a class="button" href="{{ url('app_reset_password', {token: resetToken.token}) }}">
        Réinitialiser mon mot de passe
    </a>

    <p>Ce lien expirera dans
        {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
    </p>

    <p>Merci,<br>L’équipe</p>
</div>
</body>
</html>
